name: findwin
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.2.1
  cupertino_icons: ^1.0.6
  email_validator: ^2.0.1
  http: ^0.13.3
  firebase_core: ^2.10.0
  firebase_auth: ^4.16.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Assets kısmı buraya eklenecek, sadece tek bir 'flutter:' başlığı olacak.
  assets:
    - lib/assets/images/
